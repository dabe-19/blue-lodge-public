### Feature Overview
Implement a verification scenario to validate George's tiered memory persistence (SQLite milestone archiving, FTS5 lexical recall sieve) and his ability to execute follow-up tasks on top of previously completed projects without modifying the core codebase.

### Layer Changes
None (pure validation scenario run executed entirely inside the isolated CUDA docker container sandbox).

### Scope Boundaries
This plan focuses strictly on scenario-level loop execution to verify cross-task sieve recall, script compilation, and Discord delivery. No codebase files will be altered.

### Touched Layers (Handoff Routing)
- **core-specialist**: no — pure validation run.
- **commands-specialist**: no — pure validation run.
- **ui-specialist**: no — pure validation run.
- **tests-specialist**: no — pure validation run.

### Tooling Layer (Provisioning)
- **Tooling**: no

### Functional Verification
- **Verification**: yes — sandbox execution run.

### Security
- **Security**: no

### Style
- **Style**: no
